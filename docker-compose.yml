services:
  cuda_pipeline:
    build: .
    container_name: cuda_covariance_env
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
